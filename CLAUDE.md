# CLAUDE.md — matchpass-club

## What this repo is

The static club directory site for MatchPass. Public repo under renegaid-org.

## Sentinel

Sentinel project state lives in `matchpass-prv/docs/sentinel/`. This repo is
public — **never create sentinel files or directories here**. When running
`/sentinel`, read state from `/media/sf_MintAI/MatchPass/matchpass-prv/docs/sentinel/state.yaml`.
